甲：

1.退出登录 → 再登录 teacher01/123456，登录后台管理平台显示无权限访问。登录网页端可以进入。

2.点「添加讲师」→ 填李医师 / 主任医师 / 临床 → 保存.]。显示系统繁忙，错误代码：code":500,"message":"系统繁忙，请稍后重试","data":null}。

3.点「编辑讲师」→ 改联系电话 → 保存。没有联系电话这个选项。

4.数据库验证跑以下sql代码，代码有错误，无法运行看不到结果。





乙：

5.点「添加课程」→ title=高血压诊疗规范 / type=1(公开课)/ hours=20 / offlineFlag=0 → 保存。没有看到 这个offlineFlag字段的功能。

6.contentType 选 text 并存 contentText。这个字段的功能没有实现。

7.首页 ECharts 趋势图。当前显示 mock 静态数据**，不是真实数据。

8.播放视频 / 停留 10s → 看 F12 Network。没有这个功能。请求体：`{courseId, chapterId, lastPosition, duration, completed}。等 30s 后刷新 - 查 DB：`SELECT * FROM study_record WHERE user_id=2`。后续这两步也没法完成。

9.学的课程 & 个人中心功能没有实现，显示系统繁忙，个人资料加载失败。后续这两步没法查看点侧边栏「个人中心」→「我的课程」，看 profile 页。

10.数据库验证跑以下sql代码，代码有错误，无法运行看不到结果。



丙：

11.试题管理（核心），点「添加试题」→ 选单选 → 选项 A/B/C/D → 答案=C → 难度=2(中等) → 保存。无法新增，显示系统繁忙。后续添加不同类型的题，一样显示系统繁忙。

12。读 DB：`SELECT questions FROM exam_paper WHERE exam_id=#{id}。这个sql语句运行不了，无法看结果。

13.手工数：⭐ 简单 3 题、中等 5 题、困难 2 题（合计 10）。代码里目前没有"手工数"这个功能

14.考试没有发布和草稿之间的转换，新添加的无法发布，无法在在培训平台看到考试，不知道双保险计时 + 自动阅卷（核心）的操作正常吗。

15.自动阅卷算法验证，数据库验证，sql语句无法运行，显示错误。



丁：

16.DB：`SELECT * FROM consult_record ORDER BY id DESC LIMIT 1`。sql语句运行不了，显示错误。

17.等 60 秒（演示时 sla_seconds=60）。真实代码的 `slaHours` 是 `int` 且 SQL 用的是 `INTERVAL ... HOUR`，所以**最小只能设 1（=1 小时），根本设不了 60 秒**。也就是说——按现在跑着的代码，文档里那句"等 60 秒就变红"**不会发生**，那条待回复记录**最少要等 1 小时**才会进告警列表。

18.看 DB：`SELECT reply_time, status, sla_exceeded FROM consult_record WHERE id=#{id}。sql语句运行不了，显示错误。

19.返回小程序「咨询」tab，刷新或重发。刷新的话，问题就不见了，重发一样的问题，给的还是等待人工回复。

20.小程序中没有考试。无法查看功能倒计时 + 答题，以及成绩 + 对错回看，考试记录。

21.个人中心页面中，我的课程点不开，编辑个人资料显示系统繁忙。

22.报名成功后，点击开始学习，他还是回复我请先报名。这步未完成，导致学习记录功能不知道结果。

23.wx.request 自动带 token。这个不知道怎么验证。

24.视频 `<video>` 组件播放。没有这个功能。

25.PDF 预览（wx.downloadFile + wx.openDocument），文本富文本渲染。章节详情打不开。

26. 数据库验证，sql语句运行不了，显示错误。